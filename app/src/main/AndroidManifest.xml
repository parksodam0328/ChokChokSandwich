<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.parksodam.chokchoksandwich">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu.SandwichMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".User.RegisterActivity"
            android:windowSoftInputMode="adjustUnspecified" />
        <activity
            android:name=".User.LoginActivity"
            android:windowSoftInputMode="adjustUnspecified" />
        <activity android:name=".BarActivity" />
        <activity android:name=".Menu.DrinkMenuActivity" />
        <activity android:name=".Menu.SideMenuActivity"></activity>
    </application>

</manifest>